{
  "name": "ui",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/pswui/ui",
  "author": "p-sw <shinwoo.park@psw.kr>",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "postinstall": "lefthook install",
    "react": "bun --filter react",
    "cli": "bun --filter @psw-ui/cli",
    "registry:checksums": "bun ./scripts/registry-checksums.ts",
    "react:build": "bun --filter react build",
    "react:dev": "bun --filter react dev",
    "react:test:e2e": "bun --filter react test:e2e",
    "cli:build": "bun --filter @psw-ui/cli build"
  },
  "private": true,
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "lefthook": "^1.6.18"
  }
}
