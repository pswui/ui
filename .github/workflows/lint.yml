name: lint-and-check

on: [ pull_request,push]

jobs:
  cli-check:
    runs-on: ubuntu-latest
    name: CLI Check
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json
      - name: Install dependencies
        run: |
          bun install --frozen-lockfile
      - name: Lint
        run: bun cli lint --write
      - name: Build
        run: bun cli build

  component-check:
    runs-on: ubuntu-latest
    name: Component Check
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json
      - name: Install dependencies
        run: |
          bun install --frozen-lockfile
      - name: Lint
        run: bun react lint --write
      - name: TypeScript Compile
        run: bun react tsc
